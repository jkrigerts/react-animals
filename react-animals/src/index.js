import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import App from "./App";

const railijs = ReactDOM.createRoot(document.getElementById("railijs"));
railijs.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
